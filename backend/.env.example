# MongoDB Connection
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/weight-tracker?retryWrites=true&w=majority

# Server Configuration
NODE_ENV=development
PORT=5000

# Instructions:
# 1. Copy this file to .env
# 2. Replace the MongoDB URI with your actual connection string from MongoDB Atlas
# 3. Keep the database name as 'weight-tracker' or change if needed
